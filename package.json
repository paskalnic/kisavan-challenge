{
  "name": "kisavan-challenge",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run --pool=forks --poolOptions.forks.singleFork=true",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vitest": "^1.5.5"
  }
}
