import {json,supabaseRequest,validUuid} from "../_common.js";
export async function onRequestGet(context){try{const url=new URL(context.request.url);const quizId=url.searchParams.get("quizId");if(!validUuid(quizId))return json({error:"Quiz invalide."},400);const entries=await supabaseRequest(context.env,`attempts?quiz_id=eq.${quizId}&select=alias,score,total,duration_ms,created_at&order=score.desc,duration_ms.asc&limit=50`);return json({entries:entries.map(e=>({alias:e.alias,score:e.score,total:e.total,duration_seconds:Math.round(e.duration_ms/1000)}))})}catch(error){return json({error:error.message||"Erreur serveur."},500)}}
